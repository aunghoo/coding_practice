# compilation of python syntactic usages

def maxsum(arr, l, r):
	name = raw_input("hey")
	print name
	print max(arr[0], arr[1])
	for it in arr:
		print(it)

arr = [1, 2, 4, 12, 18]
maxsum(arr, 1, 2)